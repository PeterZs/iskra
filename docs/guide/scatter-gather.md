# Scatter-Gather Dataflow in `iskra ✨`

One of the main innovations in `iskra ✨` is the _tensor-based_ scatter-gather dataflow allowing us to efficiently and concisely express mesh operation which are inherently sparse in a dense tensor framework.
It consists of three main components one must keep in mind.

## Face Hierarchy

## Face-Index (Gather)

## Reduce-on-Subface (Scatter-Reduce)
